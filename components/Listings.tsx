import { View, Text } from "react-native";
interface Props {
  listings: any;
  category: string;
}

const Listings = () => {
  return (
    <View>
      <Text>Listings</Text>
    </View>
  );
};

export default Listings;
